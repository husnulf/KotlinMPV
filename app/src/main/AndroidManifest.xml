<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="husnul.com.kotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".HitungBangunDatarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BebasActivity" />
        <activity android:name=".MVP.MainMPVActivity" />
        <activity android:name=".MPVlatihan.KumpulanRumusMPVActivity" />
        <activity android:name=".MPVlatihan.SegitigaActivity" />
        <activity android:name=".MPVlatihan.PersegiActivity" />
        <activity android:name=".MPVlatihan.PersegiPanjangActivity"></activity>
    </application>

</manifest>